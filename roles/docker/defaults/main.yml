---
# =============================================================================
# Docker Role Default Variables
# =============================================================================

# List of users to add to the docker group
# By default, includes ansible_user and target_user from global vars
docker_users: 
  - "{{ ansible_user }}"
  - "{{ target_user }}"
  # Add additional users here or override in group_vars/host_vars
  # - user3
  # - user4
